  <%= form_with(model: @video, local: true) do |f| %>

    <% if @video.errors.any? %>
      <%= "#{@video.errors.full_messages.count}件のエラーが発生しました。" %>
        <% @video.errors.full_messages.each do |message| %>
          <%= message %>
        <% end %>
  <% end %>

    <div class="video-form__field">
      <div class="video-form__field--left">
        <%= f.label :title, "動画タイトル", class: "video-form__label" %>
      </div>
      <div class="video-form__field--right">
        <%= f.text_field :title, placeholder:"例）BEATLES LET IT BE", class: "video-form__input" %>
      </div>
    </div>

    <div class="video-form__field">
      <div class="video-form__field--left">
        <%= f.label :url, "埋め込みURL", class: "video-form__label" %>
      </div>
      <div class="video-form__field--right">
        <%= f.text_field :url, placeholder:"例）https://www.youtube.com/embed/DFC1xpxTVrs", class: "video-form__input" %>
        <p>各動画ページの「共有」→「埋め込み」で表示される文字列の中から「src="〜"」の「〜」に該当する部分を抜き出してください。</p>
      </div>
    </div>

    <div class="video-form__field">
      <div class="video-form__field--left">
        <%= f.label :note, "動画情報メモ", class: "video-form__label" %>
      </div>
      <div class="video-form__field--right">
        <%= f.text_field :note, placeholder:"例）洋楽 MV", class: "video-form__input" %>
      </div>
    </div>

    <div class="video-form__field">
      <div class="video-form__field--left">
        <%= f.label :word, "キーワード", class: "video-form__label"%>
      </div>
      <div class="video-form__field--right">
        <button type="button" class="video-form__field__btn">♪音声入力♪</button>
        <%= f.text_field :word, placeholder:"キーワードがここに入力されます", class: "video-form__input" %>
        <p>「音声入力」ボタンをクリックして、設定したいキーワードを音声入力してください。自動でフォームが入力されます。</p>
      </div>
    </div>

    <div class="video-form__field--right">
      <%= f.submit class: "video-form__btn"%>
    </div>
  <% end %>